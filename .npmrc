# Forces npm to save dependencies with exact versions
# instead of using semver range operators (^, ~, etc.)
save-exact = true

# Ensures the project only runs on node versions
# that satisfy the "engines" field in package.json
engine-strict = true
